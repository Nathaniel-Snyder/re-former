
<%= form_for @user do |f| %>
	<%= f.label :username %>:
	<%= f.text_field :username, :placeholder => 'Your username here' %></br>

	<%= f.label :email %>:
	<%= f.text_field :email %></br>

	<%= f.label :password %></br>
	<%= f.password_field :password %>

	<%= f.submit %>
<% end %>